{
  # NOTE:
  # If you prefer using HM’s kitty-themes package instead, uncomment the next line
  # and delete the "include current-theme.conf" in extra.nix.
  #
  programs.kitty.themeFile = "current-theme";
}
