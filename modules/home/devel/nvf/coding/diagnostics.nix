# ── Diagnostics UI/flow (Trouble) ─────────────────────────────────────────
{
  programs.nvf.settings.vim.diagnostics = {
    enable = true;
    config = {
      virtual_line.enable = true;
      underline = true;
    };
  };
}
